.error_messages
= simple_form_for(object.comments.new, html: { class: 'form-horizontal'}, remote: true) do |f|
  = f.input :commentable_type, :as => :hidden
  = f.input :commentable_id, :as => :hidden
  = f.input :author
  = f.input :content
  = f.button :submit
